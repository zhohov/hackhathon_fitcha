import React from 'react';

const Profile = () => {

    return(
        <div class='p-3' id='profile'>
            <h4>Привет, username</h4>
            <button type="submit" class="btn btn-primary mt-2">Выход</button>
        </div>
    );
};

export default Profile;